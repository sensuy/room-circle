name: room-circle
services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: roomcircle
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?Set POSTGRES_PASSWORD in .env}
      POSTGRES_DB: roomcircle
    ports:
      - "127.0.0.1:55432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U roomcircle -d roomcircle"]
      interval: 5s
      timeout: 5s
      retries: 10
volumes:
  postgres_data:
